import type { Metadata } from "next";
import { AiRoleDetail } from "@/components/careers/ai-role-detail";

export const metadata: Metadata = { title: "AI Engineer (Contract) — Sanganak Works" };

export default function AiEngineerPage() {
  return <AiRoleDetail />;
}
